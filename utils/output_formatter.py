# instead of the report agent? Just a simple script that takes the judge decisions 
# and created the final .txt file and makes the proyect .zip to upload

# utils/output_formatter.py
import os
import zipfile

# TODO: Create `generate_submission_file(fraud_ids: list, filename="output.txt")`.
# Write the IDs exactly as requested by the challenge problem statement (usually one ID per line).

# TODO: Create `zip_project_for_submission(output_zip_name="submission.zip")`.
# Zip the entire project folder.
# CRITICAL: Exclude 'venv/', '.env', '__pycache__/', and 'data/' folders so you don't upload secrets or heavy datasets.