# agents/analyzer.py

# TODO: Define the "System Prompt" (instructions) for the Analyzer Agent.
# Clearly state it is a financial fraud detection expert.
# Instruct it that it MUST use the provided Python tools to perform calculations before answering.

# TODO: Create the analysis function.
# Format the user history so the LLM easily understands it (e.g., JSON or clear text).
def analyze_pattern(user_history, current_transaction):
    pass
